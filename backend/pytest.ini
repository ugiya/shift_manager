[pytest]
testpaths = tests
# The JVM (under Timefold's Python bridge) uses SIGSEGV for normal safepoint
# handling; pytest's faulthandler misreads those as crashes. Disable it.
addopts = -p no:faulthandler
